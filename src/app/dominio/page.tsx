import { DomainLookupTool } from "@/components/DomainLookupTool";

export const metadata = { title: "Consultar dominio — Shopify Audit" };

export default function DomainLookupPage() {
  return <DomainLookupTool />;
}
